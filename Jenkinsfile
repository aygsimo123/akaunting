pipeline {
    agent any
    stages {
        stage('Test PHP') {
        steps {
            bat 'php -v'
        }
        }
        stage('Test Composer') {
        steps {
            bat 'composer -V'
        }
        }
    }
    }
